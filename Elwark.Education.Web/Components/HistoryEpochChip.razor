@using Elwark.Education.Web.Gateways.History.Epoch
@inject IStringLocalizer<App> L

<MudChip Label="true" DisableRipple="false" Class="@Class" Color="Color.Primary" Size="Size.Small" Link="@Links.History.Epoch(Epoch)">
    @L[$"History:{Epoch}"]
</MudChip>

@code {

    [Parameter]
    public EpochType Epoch { get; set; }

    [Parameter]
    public string? Class { get; set; }
}
