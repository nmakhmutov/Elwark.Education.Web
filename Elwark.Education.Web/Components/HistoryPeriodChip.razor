@using Elwark.Education.Web.Gateways.Models.History
@inject IStringLocalizer<App> L

<MudChip Label="true" DisableRipple="false" Class="@Class" Color="Color.Primary" Size="Size.Small" Link="@Links.History.Period(Period)">
    @L[$"History:{Period}"]
</MudChip>

@code {

    [Parameter]
    public HistoryPeriodType Period { get; set; }

    [Parameter]
    public string? Class { get; set; }
}
