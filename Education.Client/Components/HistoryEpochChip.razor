@using Education.Client.Gateways.History.Epoch

<MudChip Label="true" DisableRipple="false" Class="@Class" Color="Color.Primary" Size="Size.Small" Link="@Links.Subject.History.Epoch(Epoch)">
    @L[$"History:{Epoch}"]
</MudChip>

@code {

    [Inject]
    private IStringLocalizer<App> L { get; set; } = default!;
    
    [Parameter, EditorRequired]
    public EpochType Epoch { get; set; }

    [Parameter]
    public string? Class { get; set; }
}
