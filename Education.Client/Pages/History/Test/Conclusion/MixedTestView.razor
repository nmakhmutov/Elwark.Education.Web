@using Education.Client.Gateways.History.Test

<CenteredContainer MaxWidth="ContainerWidth.W1280" Class="my-3 my-sm-6" Paper="true" Elevation="1">
    <div class="d-flex flex-wrap justify-space-between">
        <div class="pa-3">
            <MudText Typo="Typo.h5">
                @L["Test:Mixed"]
            </MudText>
        </div>
        <div class="pa-3">
            <MudText Typo="@Typo.subtitle1" Color="@Conclusion.Status.GetColor()">
                @L[$"ConclusionStatus:{Conclusion.Status}"]
            </MudText>
            <MudText Typo="Typo.body1" Align="Align.Right">
                @Conclusion.CompletedAt.ToSimpleFormat()
            </MudText>
        </div>
    </div>
</CenteredContainer>

<CenteredContainer MaxWidth="ContainerWidth.W1280" Class="mb-3 mb-sm-6">
    <div class="mb-3">
        <MudLink Href="@Links.Subject.History.Profile.MixedTestStatistics" Typo="Typo.h5">
            @L["Statistics"]
        </MudLink>
    </div>
    <ConclusionCharts Conclusion="@Conclusion"/>
</CenteredContainer>

<CenteredContainer MaxWidth="ContainerWidth.W1280" Class="mb-3 mb-sm-6">
    <MudText Typo="Typo.h5" Class="mb-3">
        @L["Answers"]
    </MudText>
    <MudTable Items="@Conclusion.Questions" Hover="true" Breakpoint="Breakpoint.Sm">
        <HeaderContent>
            <MudTh>
                @L["Topic"]
            </MudTh>
            <MudTh>
                @L["Question"]
            </MudTh>
            <MudTh>
                @L["Question:IsAnswered"]
            </MudTh>
            <MudTh>
                @L["Question:IsCorrect"]
            </MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="@L["Topic"]">
                <MudLink Href="@Links.Subject.History.Topic(context.Topic.Id)">
                    @context.Topic.Title
                </MudLink>
            </MudTd>
            <MudTd DataLabel="@L["Question"]">
                @context.Title
            </MudTd>
            <MudTd DataLabel="@L["Question:IsAnswered"]">
                <MudCheckBox ReadOnly="true" Checked="@context.IsAnswered"/>
            </MudTd>
            <MudTd DataLabel="@L["Question:IsCorrect"]">
                <MudCheckBox ReadOnly="true" Checked="@context.IsCorrect"/>
            </MudTd>
        </RowTemplate>
    </MudTable>
</CenteredContainer>


@code {

    [Inject]
    private IStringLocalizer<App> L { get; set; } = default!;

    [Parameter]
    public MixedTestConclusion Conclusion { get; set; } = default!;

}
