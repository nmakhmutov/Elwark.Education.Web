@using Education.Client.Gateways.History.Epoch
@using Education.Client.Gateways.Models.Content

<section class="card">
    <div class="media">
        <a href="@Href" class="image-link">
            <span class="bg-cover" style="background-image: url('@Image')"></span>
        </a>
        <span class="labels">
            
        </span>
    </div>
    <div class="content">
        <div class="meta">
            <h2 class="title">
                <a href="@Href">
                    @Title
                </a>
            </h2>
            <div class="d-flex flex-row justify-space-between align-center">
                <HistoryEpochChip Class="ma-0" Epoch="@Epoch"/>
                <RatingStar Rating="@Rating"/>
            </div>
            @* <div class="meta-items"> *@
            @*     @Subtitle *@
            @* </div> *@
        </div>
        <div class="excerpt">
            @* <p>@(Subtitle.Length > 80 ? Subtitle[..80] + "..." : Subtitle)</p> *@
            @* <RatingStar Rating="@Rating"/> *@
        </div>
    </div>
</section>

@code {

    [Parameter]
    public string Image { get; set; } = string.Empty;

    [Parameter]
    public string Href { get; set; } = string.Empty;

    [Parameter]
    public string Title { get; set; } = string.Empty;

    [Parameter]
    public string Subtitle { get; set; } = string.Empty;

    [Parameter]
    public EpochType Epoch { get; set; }

    [Parameter]
    public ContentRating Rating { get; set; } = default!;

}
