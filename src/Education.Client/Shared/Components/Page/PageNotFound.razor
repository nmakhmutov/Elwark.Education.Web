@using Education.Client.Clients
<PageTitle>
    @Error.Title
</PageTitle>

<EduErrorContainer Error="@Error"/>

@code {

    private Error Error =>
        Error.Create(L["Error_NotFound"], 404);

    [Inject]
    private IStringLocalizer<App> L { get; init; } = default!;

}