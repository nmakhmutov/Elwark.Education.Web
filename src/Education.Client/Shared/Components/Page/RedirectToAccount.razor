@using Education.Client.Features.Customer

@code {

    [Inject]
    private NavigationManager Navigation { get; init; } = default!;

    protected override void OnInitialized() =>
        Navigation.NavigateTo(CustomerUrl.Root);

}
