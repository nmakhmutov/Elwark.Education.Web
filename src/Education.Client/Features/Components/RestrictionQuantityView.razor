@using Education.Client.Gateways.Models.User
<MudText Class="@Class" Typo="@Typo">
    @(Restriction.Quantity < 0 ? "∞" : $"{Restriction.Quantity}/{Restriction.Threshold}")
</MudText>

@code {

    [Parameter, EditorRequired]
    public Restriction Restriction { get; set; } = default!;

    [Parameter]
    public Typo Typo { get; set; } = Typo.subtitle1;

    [Parameter]
    public string? Class { get; set; }

}