@using ApexCharts
@using Education.Client.Features.History.Clients.Examination.Model
<div class="@Class">
    <ApexChartContainer>
        <ApexChart TItem="Data" Options="@Options">
            <ApexPointSeries
                TItem="Data"
                Items="@Series"
                SeriesType="SeriesType.Donut"
                XValue="@(x => x.Name)"
                YAggregate="@(x => x.Sum(e => e.Value))"/>
        </ApexChart>
    </ApexChartContainer>

    <ChartList Items="@Items"/>
</div>

@code {

    private ApexChartOptions<Data> Options =>
        new()
        {
            Chart = new Chart
            {
                Background = "transparent",
                ForeColor = "var(--mud-palette-text-primary)"
            },
            Stroke = new Stroke
            {
                Show = false
            },
            Legend = new Legend
            {
                Show = false
            },
            Colors =
            [
                HistoryChart.Examination.Score.Questions,
                HistoryChart.Examination.Score.SpeedBonus,
                HistoryChart.Examination.Score.NoMistakesBonus
            ]
        };

    [Inject]
    private IStringLocalizer<App> L { get; init; } = default!;

    [Parameter]
    public string? Class { get; set; }

    [Parameter, EditorRequired]
    public required ScoreModel Value { get; set; }

    private ChartList.Item[] Items =>
    [
        new ChartList.Item(L["Score_Questions_Title"], Value.Questions, HistoryChart.Examination.Score.Questions),
        new ChartList.Item(L["Score_SpeedBonus_Title"], Value.Speed, HistoryChart.Examination.Score.SpeedBonus),
        new ChartList.Item(L["Score_NoMistakesBonus_Title"], Value.NoMistakes, HistoryChart.Examination.Score.NoMistakesBonus),
        new ChartList.Item(L["Score_Total_Title"], Value.Total, null)
    ];

    private Data[] Series =>
    [
        new Data(L["Score_Questions_Title"], Value.Questions),
        new Data(L["Score_SpeedBonus_Title"], Value.Speed),
        new Data(L["Score_NoMistakesBonus_Title"], Value.NoMistakes)
    ];

    private sealed record Data(string Name, decimal Value);

}
