@using Education.Client.Features.History.Clients.Learner.Model.Quiz
@if (string.IsNullOrEmpty(Tooltip))
{
    <MudChip
        Class="ma-0"
        Size="Size.Small"
        Color="Color.Secondary"
        Variant="Variant.Text"
        Icon="@EduIcons.Quiz"
        IconColor="@Color"
        Text="@NumberOfQuizzes.Succeeded.ToString()"
        Href="@Href"
        DisableRipple
        Label/>
}
else
{
    <MudTooltip Text="@Tooltip">
        <MudChip
            Class="ma-0"
            Size="Size.Small"
            Color="Color.Secondary"
            Variant="Variant.Text"
            Icon="@EduIcons.Quiz"
            IconColor="@Color"
            Text="@NumberOfQuizzes.Succeeded.ToMetric()"
            Href="@Href"
            DisableRipple
            Label/>
    </MudTooltip>
}

@code {

    [Parameter, EditorRequired]
    public required NumberOfQuizzesModel NumberOfQuizzes { get; set; }

    [Parameter, EditorRequired]
    public Color Color { get; set; }

    [Parameter]
    public string? Tooltip { get; set; }

    [Parameter]
    public string? Href { get; set; }

}
