@using Education.Client.Features.History.Clients.Learner.Model
<section class="d-flex flex-column mud-paper gap-3 pa-3 pa-sm-6 @Class">
    <div class="d-flex justify-space-between">
        <div class="d-flex align-center mr-3">
            <MudIcon Icon="@EduIcons.Examination" Size="Size.Small" Class="mr-3"/>
            <MudText Typo="Typo.body2">
                @L["NumberOfExaminations_Total_Title"]
            </MudText>
        </div>
        <EduMetricNumber Value="@Total.Examinations" Typo="Typo.body2"/>
    </div>
    <div class="d-flex justify-space-between">
        <div class="d-flex align-center mr-3">
            <MudIcon Icon="@EduIcons.Score" Size="Size.Small" Class="mr-3"/>
            <MudText Typo="Typo.body2">
                @L["Score_Total_Title"]
            </MudText>
        </div>
        <EduMetricNumber Value="@Total.Score" Typo="Typo.body2"/>
    </div>
    <div class="d-flex justify-space-between">
        <div class="d-flex align-center mr-3">
            <MudIcon Icon="@EduIcons.Questions" Size="Size.Small" Class="mr-3"/>
            <MudText Typo="Typo.body2">
                @L["Questions_Total"]
            </MudText>
        </div>
        <EduMetricNumber Value="@Total.Questions" Typo="Typo.body2"/>
    </div>
    <div class="d-flex justify-space-between">
        <div class="d-flex align-center mr-3">
            <MudIcon Icon="@EduIcons.TimeSpent" Size="Size.Small" Class="mr-3"/>
            <MudText Typo="Typo.body2">
                @L["TimeSpent_Total_Title"]
            </MudText>
        </div>
        <MudText Typo="Typo.body2">
            @Total.TimeSpent.Humanize(L)
        </MudText>
    </div>

    @if (Total.Examinations > 0)
    {
        <MudSpacer/>
        <TryPracticeQuestionsButton Link="@HistoryUrl.Content.ExaminationBuilder(CourseId)" FullWidth/>
    }
</section>

@code {

    [Inject]
    private IStringLocalizer<App> L { get; init; } = default!;

    [Parameter, EditorRequired]
    public required string CourseId { get; set; }

    [Parameter, EditorRequired]
    public required CourseStatisticsModel.TotalModel Total { get; set; }

    [Parameter]
    public string? Class { get; set; }

}
