@page "/history/date-guessers"
@layout HistoryLayout
@attribute [Authorize]

<PageTitle>
    @L["History_DateGuessers_Title"]
</PageTitle>

<EduContainer Class="py-3 py-sm-6" MaxWidth="EduWidth.W1920">
    <PageBreadcrumbsHeader Items="@Breadcrumbs"/>
</EduContainer>

<ApiViewer Result="@_response" OnReloadClick="@OnInitializedAsync">
    <EduContainer Class="pb-3 pb-sm-6" MaxWidth="EduWidth.W1920">
        <section class="grid">
            <section class="builder">
                <MudText Typo="Typo.h6" Class="mb-3">
                    @L["History_Epoch_Title"]
                </MudText>
                <EpochRadioGroup
                    SelectedEpoch="@_settings.Epoch"
                    SelectedEpochChanged="@OnEpochChanged"
                    Class="mb-3 mb-sm-6"/>

                <MudText Typo="Typo.h6" Class="mb-3">
                    @L["History_DateGuesser_Type_Title"]
                </MudText>

                <DateGuesserSizes
                    Guessers="@context.Tests"
                    OnClick="@CreateTestAsync"/>
            </section>

            <section class="inventory">
                <MudText Typo="Typo.h6" Class="mb-3">
                    @L["History_DateGuesser_Equipment"]
                </MudText>
                <InventoryGrid
                    Inventories="@context.Inventories"
                    StoreHref="@HistoryUrl.Store.Inventory(CategoryType.DateGuesser)"
                    Context="inventory">
                    <ChildContent>
                        <InventoryInfoCard
                            Title="@inventory.Title"
                            Overview="@inventory.Overview"
                            Quantity="@inventory.Quantity"
                            IconUrl="@inventory.ImageUrl"/>
                    </ChildContent>
                    <EmptyContent>
                        <InventoryShortageCard
                            Class="w-max-600"
                            Href="@HistoryUrl.Store.Inventory(CategoryType.DateGuesser)"/>
                    </EmptyContent>
                </InventoryGrid>
            </section>
        </section>
    </EduContainer>
</ApiViewer>
