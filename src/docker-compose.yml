version: '3.7'

services:

  education.web:
    container_name: "education_web"
    image: elwark/education/web:${TAG:-latest}
    build:
      context: .
      dockerfile: Education.Client/Dockerfile
    ports:
      - "5010:80"
