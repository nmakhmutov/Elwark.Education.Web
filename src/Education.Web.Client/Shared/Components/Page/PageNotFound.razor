<PageTitle>
    @Error.Title
</PageTitle>

<EduError Error="@Error"/>

@code {

    private Error Error =>
        Error.Create(L["Error_NotFound"], 404);

    [Inject]
    private IStringLocalizer<App> L { get; set; } = default!;

}