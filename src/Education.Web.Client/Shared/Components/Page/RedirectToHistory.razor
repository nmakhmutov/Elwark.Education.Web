@using Education.Web.Client.Features.History

@code {

    [Inject]
    private NavigationManager Navigation { get; init; } = default!;

    protected override void OnInitialized() =>
        Navigation.NavigateTo(HistoryUrl.Root);

}