@if (Theme is not null)
{
    <MudIconButton Icon="@Theme.Icon" Color="Color.Inherit" OnClick="@Theme.ToggleAsync"/>
}

@code {

    [CascadingParameter]
    private EduThemeProvider? Theme { get; set; }

}