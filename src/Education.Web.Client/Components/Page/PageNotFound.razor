<EduPage Title="@Error.Title">
    <EduError Error="@Error"/>
</EduPage>

@code {

    private Error Error =>
        Error.Create(L["Error:NotFound"], 404);

    [Inject]
    private IStringLocalizer<App> L { get; set; } = default!;

}