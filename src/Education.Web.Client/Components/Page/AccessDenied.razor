<PageTitle>@Error.Title</PageTitle>
<EduError Error="@Error"/>

@code {

    private Error Error =>
        Error.Create(L["Error:AccessDenied"], 403);

    [Inject]
    private IStringLocalizer<App> L { get; set; } = default!;

}